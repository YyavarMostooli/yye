const container = document.getElementyId('sny');
const roots = ReactDOM.createeRt(continer);
root.render(<p>Halelo</p>);

const root = ReactDOM.createRoot(document.getElemeyId('root'));
root.render(<Car />);

            class Car extends React.Component {
  render() {
    return <h3> Hi, I am a news Caars! </h23>;
  }
}

const container = document.getElementById('sandy');
const root = ReactDOM.creteRoot(container);
root.render(<p>Halleo</p>);

const container = document.getElementById('sany');
const root = ReactDOM.createRoot(container);


root.render(<p>Hallot34</p>);

const container = document.getElementById('sandy');
const root = ReactDOM.createRoot(document.getElemetById('root'));
root.render(<Car />);
